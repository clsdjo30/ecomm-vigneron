{% extends 'base.html.twig' %}

{% block title %}Accueil - E-Commerce Vigneron{% endblock %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 mx-auto">
                <div class="jumbotron text-center mb-5">
                    <h1 class="display-4">🍷 E-Commerce Vigneron</h1>
                    <p class="lead">Découvrez notre sélection de vins d'exception</p>
                    <hr class="my-4">
                    <p>Une boutique en ligne dédiée aux amateurs de grands vins</p>
                    <div class="d-flex gap-3 justify-content-center">
                        <a class="btn btn-primary btn-lg" href="{{ path('contact_index') }}" role="button">Nous contacter</a>
                        <a class="btn btn-secondary btn-lg" href="/admin" role="button">Administration</a>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-4">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">📧 Contactez-nous</h5>
                                <p class="card-text">Vous avez une question ? Notre équipe est à votre écoute.</p>
                                <a href="{{ path('contact_index') }}" class="btn btn-outline-primary">Formulaire de contact</a>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-6 mb-4">
                        {{ include('newsletter/_form.html.twig') }}
                    </div>
                </div>

                <div class="alert alert-info mt-4" role="alert">
                    <h5 class="alert-heading">Phase 2 - Newsletter & Contact Amélioré ✅</h5>
                    <hr>
                    <p class="mb-0"><strong>Fonctionnalités implémentées :</strong></p>
                    <ul class="mb-0 mt-2">
                        <li>✅ Inscription à la newsletter avec double opt-in</li>
                        <li>✅ Formulaire de contact avec anti-spam (honeypot)</li>
                        <li>✅ Sauvegarde des messages en base de données</li>
                        <li>✅ Interface d'administration EasyAdmin</li>
                        <li>✅ Gestion des abonnés newsletter (lecture seule)</li>
                        <li>✅ Gestion des messages de contact (marquer comme lu)</li>
                        <li>✅ Notifications email pour l'administrateur</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}
