{% extends 'base.html.twig' %}

{% block title %}Accueil - Domaine de la Gardiole{% endblock %}

{% block body %}
{# Hero Banner #}
<section class="hero-section">
    <div class="container">
        <h1 class="display-3 fw-bold mb-4">Bienvenue au Domaine de la Gardiole</h1>
        <p class="lead mb-4">Des vins d'exception depuis 1920, cultivés avec passion dans le respect de notre terroir</p>
        <a href="{{ path('app_domain') }}" class="btn btn-light btn-lg">Découvrir notre histoire</a>
    </div>
</section>

{# Section Histoire #}
<section class="container my-5">
    <div class="row align-items-center">
        <div class="col-md-6">
            <img src="https://images.unsplash.com/photo-1510812431401-41d2bd2722f3?w=600" alt="Vignoble" class="img-fluid rounded shadow">
        </div>
        <div class="col-md-6">
            <h2 class="mb-4">L'histoire de notre domaine</h2>
            <p class="lead text-muted">Depuis quatre générations, la famille Gardiole cultive avec passion et savoir-faire un vignoble d'exception.</p>
            <p>Notre domaine, niché au cœur d'un terroir unique, produit des vins qui reflètent l'authenticité de notre région. Chaque cuvée est le fruit d'un travail minutieux, alliant tradition et modernité.</p>
            <a href="{{ path('app_domain') }}" class="btn btn-wine mt-3">En savoir plus</a>
        </div>
    </div>
</section>

{# Section Vins Phares #}
<section class="container my-5">
    <h2 class="text-center mb-5">Découvrez nos incontournables</h2>

    {% if featuredProducts|length > 0 %}
        <div class="row row-cols-1 row-cols-md-3 g-4">
            {% for product in featuredProducts %}
                <div class="col">
                    {% include 'components/_product_card.html.twig' with {'product': product} %}
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="alert alert-info text-center">
            <p class="mb-0">Nos vins phares seront bientôt disponibles. Revenez nous voir !</p>
        </div>
    {% endif %}

    <div class="text-center mt-5">
        <a href="{{ path('app_category_show', {slug: 'vins-rouges'}) }}" class="btn btn-outline-wine me-2">Voir tous les vins rouges</a>
        <a href="{{ path('app_category_show', {slug: 'vins-blancs'}) }}" class="btn btn-outline-wine me-2">Voir tous les vins blancs</a>
        <a href="{{ path('app_category_show', {slug: 'vins-roses'}) }}" class="btn btn-outline-wine">Voir tous les vins rosés</a>
    </div>
</section>

{# Section Call-to-Action #}
<section class="bg-light py-5 my-5">
    <div class="container text-center">
        <h3 class="mb-3">Une question ? Envie de nous rendre visite ?</h3>
        <p class="lead text-muted mb-4">Notre équipe est à votre disposition pour vous conseiller</p>
        <a href="{{ path('app_contact') }}" class="btn btn-wine btn-lg">Nous contacter</a>
    </div>
</section>
{% endblock %}
